// Tests will be added when the app is more complete.
//
// The app's main entry point requires Rust FFI initialization
// (`RustLib.init()`) which isn't available in the standard test environment,
// so meaningful widget tests need either a mock RustLib or integration tests.

void main() {}
